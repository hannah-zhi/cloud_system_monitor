# 储能云端安全预警系统 PRD

## 1. 产品定位

“安全预警”用于集团级管理 100+ 储能场站的安全态势、SOS 风险等级和预警闭环入口。页面以首页场站矩阵为主视图，右侧提供预警清单；点击场站或预警进入单站安全诊断详情。

## 2. 目标用户

- 集团安全运营管理员：查看全集团场站风险分布和高严重度预警。
- 区域运维负责人：按场站、风险等级筛选并定位预警位置。
- 单站值班人员：进入单站详情查看 SOS 趋势、子系统风险和预警明细。

## 3. 页面结构

安全预警采用二级菜单结构：

- 一级菜单：安全预警。
- 二级菜单：安全监测、安全评估、预警管理、风险运营。

其中前三个二级菜单分别对应现有三张主页面：

- 安全监测：当前主页面，展示场站矩阵和右侧预警清单。
- 安全评估：面向全集团场站 SOS 情况的图表看板。
- 预警管理：预警清单的独立明细页，支持复杂筛选和预警详情查看。
- 风险运营：当前保留为导航入口位，未承载独立页面内容。

## 4. 场站总览

- 左侧导航：远景数智运营系统；菜单包含全景概览、安全预警、风险运营、健康管理、运营评估、交易协同、数据分析、数据运营。
- 场站筛选：搜索/下拉多选场站，支持恢复全部场站。
- 场站总览顶部工具栏与侧边栏之间需保留清晰边距，搜索场站框不得贴近或重叠侧边栏分割线。
- 排序：场站序号、SOS 指数。
- 风险筛选：高风险、中风险、低风险、健康。
- 场站卡片字段：通讯状态、额定容量 MW、额定能量 MWh、子系统数量、场站类型、SOS 进度条。
- 预警清单：固定全量 273 条预警，按左侧当前场站筛选结果同步过滤展示。
- 预警清单右上角不展示“查看详情”跳转按钮；点击场站总览或单站详情中的任意预警卡片时，均以“预警详情”页同款弹窗展示完整预警信息、处置建议和相关数据趋势，弹窗层级必须高于场站筛选栏、下拉框等所有页面控件。

## 5. 风险透视

- 顶部第一行展示风险场站 TOP5、全量场站平均 SOS、场站风险等级占比三个模块。
- 风险场站 TOP5 按 SOS 从低到高排列，每行展示场站名称、SOS 和风险色进度条。
- 全量场站平均 SOS 独立成仪表盘卡片，固定按全部有效场站计算，不随当前筛选条件变化；仪表盘颜色从红、黄到绿平滑渐变，不展示“当前风险等级”文字，刻度密度需与数字比例协调。
- 场站风险等级占比环图展示高风险、中风险、低风险、健康数量，随筛选变化；legend 与图形保持居中紧凑排布。
- SOS 透视区：
  - 场站SOS分布与全量场站SOS趋势放在同一行，场站SOS分布在左侧，全量场站SOS趋势在右侧。
  - 全量场站SOS趋势图为点线图，展示全量场站平均值、最大值、最小值，横坐标显示日期，支持 7天、15天、30天快捷窗口。
  - 日期统一使用 `MM-DD` 格式，例如 `04-01`。
  - 趋势图阈值线与场站SOS分布保持一致：`80` 为黄色虚线，`60` 为红色虚线。
  - 鼠标悬停趋势点或日期列时显示垂直对齐线，并同时展示最大值、平均值、最小值三个 SOS 数值；同一日期的三个点同步高亮；最小值曲线使用紫色。
  - 场站SOS分布柱状图，细柱样式，颜色按风险等级区分。
  - 场站SOS分布支持排序：场站编号-顺序、场站编号-倒序、SOS数值-从低到高、SOS数值-从高到低。
  - 柱状图支持固定可见的横向滚动条，横坐标显示场站号。
  - 横坐标区域不展示缩略曲线，仅保留场站号刻度。
  - 鼠标悬停柱子时展示完整场站名称和 SOS 数值，SOS 数值颜色与柱子风险色一致，并以单柱宽度附近的半透明竖向背景高亮当前柱子。
- 预警透视区：
  - 预警等级占比：以饼图统计全部、一级、二级、三级中的等级构成。
  - 预警模块分布：以饼图统计电池系统、电气系统、环控系统、消防系统。
- 预警类型 TOP5：按预警名称/类型聚合数量，使用进度条列表展示，数量需有差异；配色依次为红、黄、绿、青、紫。
- 图表数据随左侧场站筛选条件同步变化。

## 6. 预警详情

- 页面采用顶部横向筛选条 + 全宽预警列表布局，参考风险预警列表样式。
- 顶部 3 张预警透视图需与下方表格共用同一套筛选条件，随时间、等级、模块、预警名称、场站、位置、来源筛选结果同步变化。
- 预警类型 TOP5 的排序、进度条长度与右侧数字均基于当前筛选结果的真实聚合计数，不得叠加演示用修饰数值。
- 默认全量场景下，预警类型 TOP5 需呈现递减分布，不应出现前 5 名数量完全相同的平均化结果。
- 查询栏包含时间、等级、状态、模块、预警名称、场站、位置、来源筛选；“状态”筛选框位于“等级”后方，并需比等级筛选框更宽。
- 等级、模块、来源支持多选下拉；预警名称、场站、位置支持可搜索的多选下拉。
- 预警详情页多选下拉框与场站总览“搜索场站”下拉框保持同一组件样式，包括触发框、箭头、搜索框、选项行、勾选态、阴影和图层优先级；后续全系统多选/搜索下拉组件统一复用该样式。
- 时间筛选采用开始日期、结束日期范围选择。
- 表格字段：等级、预警名称、模块、场站、位置、事件时间、预警时间、来源。
- 表格需按 `预警名称 + 模块 + 场站 + 位置 + 来源` 聚合同类预警，同一组合只展示一行；云端与站端来源不得合并到同一行或同一详情卡片内；等级、事件时间、预警时间、状态均取该组合中最新一条预警的数据。
- 表格新增“状态”列，状态流转规则：
  - 初始生成：`待处理`。
  - 首次处理选择关闭：`关闭-误报` / `关闭-数据异常` / `关闭-其他`。
  - 首次处理选择下发 SR：`排查中`。
  - SR 完成返回后再次关闭：`关闭-准确` / `关闭-类型不准确`；如所选失效模式仍需根因补充，则先进入 `关闭-待补充根因`，根因提交完成后再变更为最终关闭状态。
- 所有 `关闭-XXX` 状态标签统一使用灰色样式。
- 一部分来源为 `站端` 的预警初始状态为 `关闭-站端已处理`；如该站端预警存在关联云端预警，则关联云端预警自动同步为 `关闭-站端已处理`。
- 下发 SR 并完成返回后，表格在“状态”列之前展示信封标记，提示该聚合预警存在 SR 回执。
- 预警管理表格列宽策略：压缩“等级、模块、来源”列宽，优先保证“预警名称、场站、位置”三列的展示空间，事件时间与预警时间列保持稳定宽度，不随其它列调整而被过度挤压。
- 预警/告警列表表格最前方展示“编号”列，不再使用从 `1` 开始的临时序号；编号格式为 `来源前缀-日期5位编号`，其中云端预警为 `CW`、站端预警为 `SW`、设备告警为 `EA`，例如 `CW-2026010200001`。
- 预警/告警列表存在 SR 返回的聚合预警时，信封图标展示在“状态”列状态标签之前，不占据“编号”列位置。
- SR 返回信封图标需位于“预警时间”和“状态”两列之间的状态列前缀区，采用绝对定位，不参与状态标签排版；无论信封是否出现，状态文字和表头“状态”保持同一左对齐位置。
- 状态列需预留左侧前缀留白，确保 SR 返回信封图标完全落在状态列内部，不遮挡“预警时间”列内容。
- “编号”列需适当收窄，仅保留 `CW/SW/EA-日期5位编号` 的单行展示空间，把更多横向空间留给预警名称和状态列。
- 预警/告警列表状态列需保留足够宽度，保证“信封图标 + 状态标签”完整展示并避免过度挤压。
- 预警/告警列表“来源”列需保证“云端预警、站端预警、设备告警”单行展示，不得因列宽过窄拆成两行。
- 预警管理页仅包含“云端预警、站端预警、设备告警”，不包含“站端告警”或“数据告警”。
- 预警管理页顶部四个统计卡标题统一使用“预警/告警”表述：`预警/告警等级占比`、`预警/告警来源分布`、`预警/告警模块分布`、`预警/告警类型 TOP5`；来源分布卡按云端预警、站端预警、设备告警三类统计，颜色需与来源标签一致。
- 预警管理页顶部环图必须使用正方形 canvas 坐标系渲染，避免被 CSS 拉伸为椭圆。
- 预警管理页顶部环图与 legend 需整体居中，legend 内文字与数字采用紧凑固定列布局，不得在外接显示器或 67% 页面缩放下被拉开或裁切。
- 点击任意聚合预警行后，以弹窗卡片形式展示该组合下多条不同时间的预警；上方使用行式列表展示同组预警，最新预警在最上方并按预警时间倒序排列，点击不同行后下方相关数据趋势图随之切换，不在页面底部固定展示详情。
- 同组预警行上方单独展示模块、场站、位置三个信息框；预警行字段顺序为：序号、等级、事件时间、预警时间、关闭时间、持续时长、状态；序号和等级列需适当加宽并居中对齐，持续时长列需缩窄，事件时间、预警时间和关闭时间列需加宽，序号直接展示 `1/2/3/4...`，不展示“来源”和“位置”列，避免列间重叠。关闭时间在预警关闭时自动填充，未关闭时留空。前几条风险预警列表卡片需提供多条不同时间、不同等级的同组预警行，点击不同行后下方趋势图切换。
- 预警详情弹窗突出展示预警名称和处置建议；预警来源保持顶部主卡左上角三角形式，文字方向斜 45 度，文字本身采用类似等级标签的小型描边 pill 风格，云端为蓝色系、站端为绿色系；顶部主卡不再展示“一级”“电池系统”等标签。如存在关联的云端/站端预警，顶部主卡右下角展示关联跳转按钮，按钮颜色与目标关联预警来源匹配：云端为蓝色，站端为绿色。详情卡片下方不再展示独立的来源、事件时间、预警时间、持续时长、当前状态信息框。
- 预警来源三角只保留三角背景和外框线，不在三角内额外叠加方框；云端/站端文字字号需略大于此前版本以保证可读性。
- 预警详情弹窗顶部底色需匹配预警等级：一级为红蓝渐变，二级为黄蓝渐变，三级为绿蓝渐变。
- 预警详情页所有完整时间显示到秒，格式为 `YYYY/MM/DD HH:mm:ss`。
- 弹窗包含可交互趋势曲线，用于展示该预警相关数据在事件前后的变化；曲线下方提供“关闭”“下发 SR”和“分析”按钮，不再展示总入口“处理”按钮。
- 点击“关闭”或“下发 SR”后，所有后续处理内容必须在新的居中弹窗中展示，不在详情弹窗底部展开，避免用户需要下拉才能看到处理步骤。
- 处理流程：
  - “关闭”直接进入关闭预警表单；“下发 SR”直接进入 SR 表单，不再先展示二次选择页。
  - 处理弹窗中不展示“请选择本组预警闭环路径”等说明小字。
  - 选择“关闭预警”时，需进一步选择 `误报`、`数据异常`、`其他`；选择 `其他` 时必须填写具体原因。
  - 选择“下发 SR”时展示 SR 表单，弹窗最上方固定展示 `场站`、`位置`、`模块`、`预警名称` 四个上下文字段；表单第一行为 `SR编号`，第二行为 `操作指导`，第三行为附件，第四行为 `SR下发时间` 与 `期望完成时间`；不展示“工单编号”；`SR下发时间` 不支持手动选择，直接显示打开“下发 SR”时的北京时间；“操作指导”默认空白。
  - SR 下发后预警状态进入 `排查中`；待 SR 完成返回后，聚合预警行出现信封标记，再次进入预警卡片可选择关闭。SR 返回确认弹窗第一行展示 `SR编号` 与 `关联工单编号`，第二行为只读回传的确定性 `排查结论`，例如“现场已完成排查，确认该Pack存在铜排螺栓松动”，第三行为 `类型准确` / `类型不准确` 确认项，最下方为预警相关失效模式下拉框；下拉框包含若干与预警名称关联的失效模式和 `其他`，选择 `其他` 时必须手动输入具体失效模式。
- 对于部分仍需进一步归因的失效模式，点击确认关闭后状态先变为 `关闭-待补充根因`，并在预警卡片下方展示根因填写框；只有根因填写完成并提交后，对应状态才变为 `关闭-准确` 或 `关闭-类型不准确`。
- 已完成 `类型准确/类型不准确` 和失效模式选择后，再次点击“下发 SR”时不再重复展示选择表单，而是直接展示已选择的 SR 返回结果；只有状态为 `关闭-待补充根因` 时，结果弹窗底部展示根因补充输入。
- 对于 `关闭-站端已处理` 的预警，详情卡片在闭环摘要内展示处理动作与排查结论，并禁用“关闭”“下发 SR”按钮，无需再次处理。
- 预警详情页数据来源与场站总览右侧预警清单一致，均从固定 273 条全量预警中按当前场站范围过滤。

## 7. 预警清单规则

- 顶部标签：全部、一级、二级、三级；数字随当前场站筛选和时间筛选变化。
- 等级分布：全量 273 条中一级 35、二级 70、三级 168。
- 预警管理页来源枚举：云端预警、站端预警、设备告警。
- 集中监测首页右侧预警清单来源枚举：云端预警、站端预警、设备告警、数据告警；数据告警仅在首页预警清单和弹窗中出现，不进入预警管理页。
- 来源颜色：云端预警蓝色，站端预警绿色，设备告警紫色，数据告警青色。
- 预警清单来源统计框的边框与标签颜色需与对应来源标签保持一致。
- 时间：显示 `MM-DD HH:mm`，不显示 `(+08:00)`。
- 场站名称格式：与场站卡片保持一致，如 `K-0005远景乌兰察布电网侧储能`。
- 时间筛选：默认全部；快捷按钮为全部、最近30天、最近7天、最近3天；支持手动日期范围。
- 预警内容来源：`risk_list_demo.xlsx`，字段包括预警名称、模块、风险等级、位置格式。
- 预警位置：鼠标悬停或聚焦预警卡片时展示。

## 8. 数据规则

- 场站基础数据来自 `site_config.xlsx`，有效场站需同时具备项目号、项目名称、额定容量、额定能量、总套数。
- 多行场站按项目号聚合，总套数累加。
- 场站名称中的英文括号统一替换为中文括号。
- 已剔除信息不完整的场站：`待定`、`K-北方片区`。
- 当前有效场站总数为 158。
- 场站类型按比例生成：配套储能约 85%，独立储能约 10%，工商业储能约 5%。

## 9. SOS 风险规则

- SOS < 60：高风险。
- 60 <= SOS < 80：中风险。
- 80 <= SOS < 100：低风险。
- SOS = 100：健康。
- 首页目标分布：健康 21 个，其余数量向低风险和中风险倾斜。
- SOS 展示：100 显示为 `100`；其他值保留两位小数。

## 10. 预警位置生成规则

- 子系统编号范围：`1 - 场站总套数`。
- Rack：形如 `101` 或 `201`，首位为 1 或 2，后两位范围 01-12。
- Pack：1-8。
- Cell：1-28。
- 非电池系统位置保持模板结构，例如 `LCC01`、`BankA-TCQ01`、`AC101`，仅替换子系统编号。

## 11. 单站详情

- 从场站卡片或预警卡片点击进入。
- 单站详情页顶部不展示“集团安全中心 / 安全预警 / 场站名称”面包屑。
- 顶部右侧保留通讯状态、风险等级、SOS 标签，风险等级和 SOS 数值颜色需按照高风险红色、中风险黄色、低风险绿色、健康蓝色匹配。
- 顶部额定功率、有功功率、剩余电量、SOC、当前告警指标条不展示。
- 页面主体采用左图表 + 右预警清单布局；左侧为 SOS 仪表盘、历史趋势、风险等级分布、子系统 SOS、设备部件箱线图和详情表格；右侧为当前场站预警清单，样式与场站总览页右侧预警清单保持一致，并支持等级切换、时间快捷筛选、自定义日期范围和来源统计联动。
- 右侧预警清单底部需与左侧 SOS 安全指数详情表格底部对齐。
- 单站右侧预警清单卡片不再悬停展示“预警位置”浮层。
- 单站详情页所有仪表盘、饼图、柱状图、曲线图和表格的深色科技风、风险配色、面板线框、标题样式、标签样式需与风险透视和预警详情保持一致。
- 单站详情页 SOS 仪表盘使用与风险透视“全量场站平均 SOS”一致的刻度式半圆仪表盘，不使用块状扇形仪表盘。
- 单站详情页 SOS 仪表盘数值使用固定浅色，不随风险等级变化；数值需位于半圆内部的视觉中心。
- 单站详情页曲线图、柱状图、箱线图需支持与风险透视一致的悬停交互：当前点/柱高亮，并展示对应日期、子系统、SOS 或统计参数。
- 单站详情页子系统 SOS 柱状图使用细柱样式，柱间距需大于柱宽，避免视觉拥挤。
- 单站详情页历史趋势图不显示“高风险/中风险”阈值文字，也不显示 y 轴标题；x 轴日期始终使用 `MM-DD` 格式，窗口变长时仅降低标签密度，不改变格式。
- 单站详情页子系统 SOS 柱状图右上角使用下拉排序，选项包括子系统编号-顺序、子系统编号-倒序、SOS数值-从低到高、SOS数值-从高到低；同时展示高风险、中风险、低风险、健康 legend。
- 单站详情页子系统 SOS 柱状图 x 轴标签使用 `#01`、`#02` 这类带 `#` 的两位编号格式。
- “设备部件 SOS 指数分析”为箱线图，包含上边缘、上四分位、中位数、均值、下四分位、下边缘和离群点；悬停箱体展示关键统计参数，悬停离群点展示具体子系统信息。
- “设备部件 SOS 指数分析”箱线图各箱体之间需保留清晰间距，避免贴得过近。
- “SOS 安全指数详情清单”和上方子系统 SOS 柱状图必须使用同一份子系统 SOS 数据源；表格中的每个子系统编号和 SOS 数值需与柱状图中的对应柱子一致。
- “SOS 安全指数详情清单”默认按照当前 SOS 指数从低到高排序；前三列列名支持点击切换升序/降序排序。
- “SOS 安全指数详情清单”表头需冻结，表头背景使用与“预警详情”表格一致的半透明深色；仅子系统、当前 SOS 指数、更新日期三列支持排序，风险描述和处置建议不提供排序按钮，排序切换时列宽不得跳动；风险描述不得出现“超长字段”等临时占位文案。

## 11.1 2026-05-08 预警管理补充规则

- 已关闭预警在详情弹窗首页直接展示闭环摘要，包括 `SR编号`、`操作指导`、`排查结论`、`预警准确/不准确`、`失效类型`；仅当状态为 `关闭-待补充根因` 时继续展示根因补充输入框，不需要补充根因的已关闭预警再次点击处理时不展示根因输入。
- 根因补充完成后，根因内容需并入详情弹窗首页闭环摘要；无需补充根因的预警在处理结果弹窗中不展示“根因/无需补充根因”字段。
- 根因摘要在闭环摘要区内独占整行展示，避免短卡片导致内容拥挤。
- 对于存在云端/站端关联关系的预警，任意一侧发生处理状态、SR 字段、排查结论、预警准确性、失效类型、根因等闭环字段变更时，另一侧关联预警需同步更新。
- 对于 `关闭-站端已处理` 的预警，不再单独展示“站端排查动作/站端排查结论”两个框；闭环摘要中将“操作指导”改为“处理动作”，内容取站端排查动作，“预警准确性”填写为 `类型准确`，失效类型按预警相关失效模式正常填充。
- 已下发 SR 且状态为 `排查中`、但 SR 尚未返回时，再次点击“处理”应直接展示“模拟 SR 完成返回”入口，不再展示“关闭预警 / 下发 SR”的第一步选择。
- `K-0005` 场站前两条同组预警保留为可演示处理流程，不默认设置为 `关闭-站端已处理`。
- 预警详情筛选栏中 `状态` 筛选框位于 `等级` 后方，触发框保持紧凑，下拉面板可宽于触发框并完整承载状态文案；筛选栏不得出现横向滚动条，且下拉面板不得被表格表头遮挡。
- 风险预警列表中的 `等级` 表头与等级标签必须在同一列中水平居中对齐，普通浏览模式不保留选择框占位。
- 预警详情卡片内的事件时间、预警时间、关闭时间三列适当减小列宽，保证持续时长和状态列可见且不重叠。

## 12. 视觉规范摘要

- 深色科技风，参考安全诊断页面。
- 重点色：蓝色用于导航和主强调；红/黄/绿用于风险等级；绿色用于健康和站端来源。
- 卡片边框和面板使用低透明度线框、微渐变背景、左侧蓝色强调线。
- 主体验不使用浮夸装饰，强调信息密度、层级和稳定布局。

## 13. 2026-05-13 集中监测兼容迭代

- 原“安全预警 / 安全监测”页面升级为一级菜单“集中监测”，位置在“全景概览”之后；“安全预警”下保留“安全评估”“预警管理”“风险运营”。
- “集中监测”继续复用原安全监测的场站矩阵、筛选和右侧预警清单能力，作为集中监控模块的兼容入口。
- 集中监测场站卡片左上角新增充放电状态标签，状态枚举为：充电、放电、待机、停机。
- 集中监测场站卡片保留原基础字段：通讯状态、额定能量/容量、子系统数量。
- 集中监测场站卡片去除 SOS 指数展示和场站类型字段。
- 集中监测场站卡片新增三项运行指标：场站 SOC、场站有功功率、剩余电量。
- 集中监测顶部不展示 SOS 范围筛选条。
- 集中监测场站卡片边框颜色与右上角状态点按通讯状态映射：通讯中断为红色，部分通讯中断为黄色，通讯正常为绿色。
- 集中监测场站卡片默认按场站编号顺序排列，页面不展示排序选项栏。
- 集中监测首页场站卡片上方不再展示“通讯正常、部分通讯中断、通讯中断”三个标签，改为三个可点击环图：SOS指数（高风险、中风险、低风险、健康）、场站内最高预警等级（一级、二级、三级、无预警）、通讯状态（通讯正常、部分通讯中断、通讯中断、离线）。
- 集中监测首页三个环图标题分别为“SOS”“站内最高预警/告警等级”“通讯状态”。
- 点击集中监测首页环图扇区或 legend 均需筛选下方场站卡片；“清空筛选”恢复全部场站。
- 通讯状态新增“离线”；离线场站卡片按离线颜色展示通讯状态，且卡片左上角不展示充电、放电、待机、停机等运行状态标签。
- 集中监测演示数据中约一半场站设置为“离线”，用于验证离线筛选、通讯状态环图、卡片右上角状态点和运行状态标签隐藏逻辑。
- 集中监测首页搜索框右侧提供排序选项，默认“SOS数值-从低到高”，并支持“SOS数值-从高到低、场站编号-顺序、场站编号-倒序”。
- 集中监测首页排序选项需与搜索场站下拉控件保持一致：点击箭头或文字均可展开/收起，展开后箭头方向翻转，下拉选项文字需保留足够左边距。
- 集中监测首页场站卡片需展示 SOS 数值和水平进度条，卡片顶部颜色依据 SOS 风险等级确定；右上角圆点颜色仅依据通讯状态确定。
- 集中监测首页右侧面板标题为“预警/告警清单”，来源统计四个框支持点击筛选下方清单，再次点击同一来源取消筛选。
- “数据告警”的来源框、标签和角标采用橘黄色系，需与“云端预警”的蓝色明显区分。
- 集中监测首页右侧“预警/告警清单”需使用自适应滚动区域，滚动到底时最后一条信息必须完整可见，并与场站卡片区域底部保持视觉对齐。
- 点击单场站卡片后进入单站详情，顶部横向标签页默认为“场站概览”，第二个标签页为“安全诊断”，并在其后提供“健康管理”标签。
- “健康管理”当前仅作为假按钮展示，不触发页面切换。
- “场站概览”页最上方新增三张指标卡：安全指数、健康指数、当前可用电量；三张卡均采用左侧图形、右侧文字与数值的横向结构。安全指数左侧为 SOS 仪表盘，健康指数左侧为护盾图标，当前可用电量左侧为电池图标，图形尺寸、标题位置和数值层级需保持一致。
- “场站概览”页顶部三张指标卡不展示额外卡片标题，右侧文字与数值需居中收拢，避免右侧大面积留白；数值字号保持克制，SOS 仪表盘内“SOS”标签需清晰可读。
- “场站概览”右侧保留原单站预警栏，左侧展示场站运行、场站属性、储能系统、拓扑图、场站有功功率和场站充放电表现。
- “场站概览”右侧单站栏标题统一为“预警/告警清单”，来源统计包含“云端预警、站端预警、设备告警、数据告警”四类，并支持点击来源框筛选下方单站清单。
- 安全评估页“风险场站 TOP5”中的任一场站条目点击后，直接进入该场站的单站页并默认打开横向标签“安全诊断”。
- “场站运行”以半圆仪表盘形式展示场站 SOC，需包含 0%/100% 刻度、指针、百分比单位，并展示场站运行状态与实时出力。
- 原“储能系统”顶部卡片改为“充放电统计”，右上角提供“当日/当月/当年”时间窗口选项，默认“当日”；点击不同时间窗口后需切换按钮高亮并刷新充电能量、放电能量数值；卡片内以双圆形水波纹分别展示充电能量和放电能量。
- “场站运行”“场站属性”“储能系统”三张顶部卡片的图表尺寸需小于文字列承载宽度，仪表盘/蓄水池不得挤压标题、指标名称或数值。
- “场站运行”SOC 仪表盘和“储能系统”SOC 蓄水池需采用稳定等比图形实现，图形、刻度、指针/水位、数值和标签必须固定在同一坐标体系内，避免因面板尺寸变化产生错位。
- “场站运行”SOC 数值不放在仪表盘深色区域内部，需参考集中监控样式放在仪表盘下方读数行，保证白色数值清晰可读；右侧“场站运行状态”“场站实时出力”采用两组竖排指标，减少无效留白。
- “场站运行”卡片内部仪表盘与右侧文字需作为固定宽度组合居中偏右排布，避免左侧拥挤和右侧大面积留白；0%/100% 刻度需避开仪表盘弧线，不得与图形重叠。
- “场站属性”指标区与标题需保持清晰上间距，避免指标文字贴近标题。
- “场站属性”数字字号和行距需保持紧凑，最后一行与卡片底部保持留白。
- “场站属性”展示项为：场站类型、系统数量、额定容量、额定功率；不展示剩余电量。
- “储能系统”中的“当日充电量”“当日放电量”必须显示确定性数值和单位，不得使用 `--` 占位。
- “储能系统”卡片中的 SOC 蓄水池与充放电指标需整体居中，避免全部贴左。
- 拓扑图下方子系统卡片数量必须与该场站“系统数量/子系统数量”一致，不得固定为默认套数。
- “场站概览”拓扑图中的子系统继续采用前一版子系统卡片网格排布，保留子系统名称、状态、有功功率、SOC、SOH 和条形状态显示。
- “场站概览”拓扑区域维持前一版子系统卡片网格排布，不展示电网、升压站、风电配套图标或母线图；子系统数量需与场站子系统数量一致；鼠标悬停任一子系统时展示该子系统内预警/告警摘要。
- 拓扑区域需展示线束关系：左上角总线/主干线连接到每一个子系统卡片，卡片换行或数量变化时线束需跟随实际卡片位置重绘。
- 多排子系统时线束需在行间可见，不得被子系统卡片遮挡；左上角总线需按实际行数引出竖线，每一排总线均连接到顶部横杠。
- 拓扑图默认一排展示 6 个子系统；左上角总线汇聚区域不得出现线束交叉。
- 若场站子系统只有一排且不足 6 个，子系统卡片组需整体居中显示。
- 子系统卡片颜色按子系统内最高预警/告警等级映射：一级红色、二级黄色、三级绿色、无预警/告警灰色；悬浮窗颜色与卡片等级保持一致。
- 子系统悬浮预警/告警信息采用结构化浮层：上方展示预警/告警总数，下方罗列预警/告警名称；浮层层级需高于拓扑卡片和周边面板，不得被遮挡。
- 子系统数量较少时，拓扑面板高度需随内容收缩，避免下方出现大面积空白。
- 拓扑图下方子系统卡片采用紧凑字号，避免有功功率、SOC、SOH 数值过大导致比例异常。
- 拓扑图中的电网、升压站、风电配套节点需使用对应设备图标，不得只用普通文字块。
- 拓扑图设备 icon 需采用统一灰色调、细线设备结构与统一等距底座，电网塔、升压站、风电配套需分别有可识别的设备细节；电网塔线条不得交错混乱，风机三叶片夹角需互为 120° 且支撑杆垂直于底座；节点底座中心需与连接线对齐，避免图标和连线错位。
- 拓扑图风电配套标签需避开连接虚线，不得遮挡分支线。
- 储能系统顶部筛选栏仅展示子系统下拉框，不额外展示“子系统”文字标签。
- 拓扑图电网、升压站、风电配套 icon 需分别保存为项目内独立 SVG 资产，路径位于 `assets/topology-icons/`，页面通过资源引用使用，避免把复杂矢量图形散落在样式伪元素中。
- 拓扑图区域不展示“显示场站结构”复选项。
- 拓扑图下方子系统卡片不展示“更多”入口。
- “场站有功功率”和“场站充放电表现”右上角需采用“风险预警列表”同款日期范围筛选栏，支持开始/结束日期日历选择；调整任一日期时另一日期需按原时间窗口长度联动平移，且起始时间不得晚于结束时间；选择后同步更新图表时间窗口，下方展示 X 轴时间刻度和 legend；鼠标悬停时展示当前日期下各曲线/柱状指标的具体数值。
- “安全诊断”标签页中的“子系统风险等级分布”饼图/环图需采用中等尺寸，避免撑满卡片或压缩 legend。
- “安全诊断”标签页中的“子系统风险等级分布”环图必须保持正圆，不得因 CSS 宽高非等比缩放变为椭圆。
- “安全预警 / 安全评估”中的“场站风险等级占比”饼图需保持合适比例、正圆且完整显示，不得裁切边缘。
- “场站风险等级占比”饼图尺寸参考中等卡片展示比例，需比最小修复版本略大但仍完整可见。
- 原单站 SOS 诊断内容移入“安全诊断”标签页，并去除右侧单站预警栏。
- 剩余电量计算规则：`剩余电量(kWh) = 额定能量(MWh) * 场站SOC / 100 * 1000`；数值保留两位小数。
- 场站有功功率当前沿用系统已有 active power 数据，按 kW 展示；待机和停机场站展示为 0.00 kW。

## 14. 2026-05-21 集中监测卡片与子系统状态规则

- 集中监测首页的单场站卡片保留 SOS 进度条，并在其下新增“健康度评分”进度条；健康度评分以 90 分左右为基准，按场站风险做轻微浮动。
- 集中监测首页的单场站卡片不再展示“场站有功功率”和“剩余电量”，改为新增 `SOP/SOE` 指标，格式为 `20MW/43MWh`，数值基于额定功率/额定能量按约 90% 派生，且应略低于额定值。
- 单场站“场站概览”拓扑图中的子系统卡片右上角展示 `充电 / 放电 / 待机 / 停机` 状态；同一场站内不得同时出现充电和放电状态。
- 子系统运行状态按场站运行状态派生：多数子系统与场站主状态一致，少量子系统可为待机或停机，用于体现局部差异。
- 子系统卡片中的条形进度应与 `系统SOH` 数值基本对应，而不是跟随 `系统SOC`；SOH 数值与进度条需保持视觉一致。
- 集中监测首页的健康指标文案统一为“健康度”，其文字、数字和进度条需与 SOS 行保持列对齐；健康度进度条采用独立紫色系，避免与 SOS 风险色混用。
- 集中监测首页的 `SOP/SOE` 指标文案统一为“场站SOP/SOE”。
- 离线场站进入单站详情后，拓扑图内所有子系统左上角状态统一显示“离线”；非离线场站的子系统左上角状态以不同颜色区分充电、放电、待机、停机，并与首页运行状态标签建立视觉对应。
- 子系统卡片的条形进度恢复为与 `系统SOC` 数值对应。
- 集中监测首页左侧场站卡片滚动区与右侧“预警/告警清单”需共用同一工作区高度，两个区域的底端必须视觉对齐；左侧卡片区内部滚动，右侧清单在同一高度内滚动。
- 集中监测首页场站卡片需完整展示所有指标行，不能因工作区内部滚动或卡片 `overflow` 裁切“场站SOP/SOE”等底部内容；指标区采用固定左右两列，右侧数值统一右对齐且保持单行展示，尤其“额定能量/容量”和“场站SOP/SOE”。
- 单站“场站概览”页左侧“场站运行 / 场站属性 / 充放电统计 / 拓扑图”等面板高度不得被右侧“预警/告警清单”数量撑高；右侧清单需要与集中监测首页右栏一致，在固定面板高度内滚动，仅清单区域滚动并保留底部留白。
- 单站“场站概览”页右侧“预警/告警清单”底端需与左侧内容底端对齐，清单数量多时仅在右栏清单区域内滚动，不改变左侧面板比例。
- 集中监测首页在筛选后即使只剩 1 个或少量场站，场站卡片也必须保持固定宽高比例，不得因网格剩余高度被拉伸。
- K-0005 等单站清单数量较多时，右侧“预警/告警清单”需固定在视口可用高度内并出现纵向滚动条；左侧“场站概览”各面板必须按内容高度排列，不得被右侧清单高度拉伸。
- 单站“场站概览”左右栏高度同步以左侧自然内容高度为准：左侧面板保持内容高度不拉伸，右侧“预警/告警清单”通过同步高度与左侧底边对齐，清单内容超出时仅清单区域出现纵向滚动条。
